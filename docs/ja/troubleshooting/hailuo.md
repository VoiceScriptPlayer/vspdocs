## 🚫 Hailuo 利用上限超過 (HailuoQuotaExceeded) {#quota-error}

### 原因  
- このリクエストで必要な文字数が、**現在残っている月間 Hailuo クォータを超えています。**  
- Patreon の支援ティア、または Free プランで提供される Hailuo の使用量をすべて使い切っています。  
- 1 回のリクエストで非常に長いテキストを送信すると、上限にすぐ到達する場合があります。

---

### 解決方法

1. **月間リセットを待つ**
    - Hailuo の使用量は **毎月 1 日** に自動でリセットされます。
    - リセット後は正常にリクエストを処理できます。

2. **Patreon の支援ティアをアップグレードする**
    - Hailuo の利用量を増やしたい場合は、上位の Patreon ティアをご検討ください。

    🔗 **Patreon メンバーシップ**  
    https://www.patreon.com/VoiceScriptPlayer/membership

3. **Hailuo を直接契約する**
    - Voice Script Server API を使用せず、Hailuo API を直接購読することで、より大きな使用量を確保できます。
    - 大量の音声生成や開発用途では、直接契約の方が適している場合があります。

    🔗 **Hailuo 直接連携ガイド**  
    https://voicescriptplayer.github.io/vsp-docs/ko/ai/hailuo-subscription/

4. **Voice Script Server API の使用量と設定を確認する**
    - プログラム設定で API キーが正しく登録されているか確認してください。  
    - API キーが無効または非アクティブの場合、使用量の取得に失敗しエラーが発生することがあります。

---

## 💡 ネットワーク接続エラー (NetworkConnectionError) {#network}

### 原因  
- インターネット接続が不安定、または切断されています。  
- Firewall、VPN、Proxy が Hailuo サーバーへの接続を遮断しています。  
- Hailuo サーバーが一時的に応答していない可能性があります。

### 解決方法  
1. **インターネット接続を確認する**
2. **Firewall や VPN を確認する**
    - `https://api.minimax.io` がブロックされていないか確認してください。
    - VPN/Proxy を無効化して再試行してください。
3. **時間をおいて再試行する**

---

## 💡 認証エラー (HailuoAuthError) {#auth-error}

### 原因  
- Hailuo API キーが間違っている、または期限切れです。  
- Authorization ヘッダーが無効な形式、または欠落しています。

### 解決方法  
1. **API キーが正しいか確認する**
2. **新しい API キーを発行する**
3. **Voice Script Server を使用している場合**
    - Patreon 連携の API キーが有効であることを確認してください。

---

## 💡 タイムアウト (HailuoTimeoutError) {#timeout-error}

### 原因  
- Hailuo サーバーの応答がタイムリミット内に返ってきませんでした。  
- 一時的にサーバー負荷が高くなっています。

### 解決方法  
1. **しばらく待って再試行する**
2. **ネットワークの安定性を確認する**
3. **テキストを短くする**

---

## 💡 リクエスト制限 (HailuoRateLimitError) {#ratelimit-error}

### 原因  
- 短時間に大量のリクエストを送信しました。

### 解決方法  
1. **1〜2 分待って再試行する**
2. **リクエスト間隔を広げる（1 秒以上推奨）**

---

## 💡 TPM（1 分あたりの処理量）超過 (HailuoTPMError) {#tpm-error}

### 原因  
- 1 分あたりの処理可能文字数の上限を超えました。

### 解決方法  
1. **テキストを分割する**
2. **リクエスト間隔を長くする**

---

## 💡 不正な文字が含まれている (HailuoIllegalCharacterError) {#illegalchar-error}

### 原因  
- テキストに絵文字や特殊記号が含まれています。

### 解決方法  
1. **絵文字・特殊記号を削除する**  
    例：💕 ✨ 🔥 ❌  
2. **テキストを簡潔にする**

---

## 💡 入力形式エラー (HailuoInvalidInputFormat) {#invalidinput-error}

### 原因  
- 必要な `voice_setting` または `audio_setting` が正しくありません。  
- テキストまたは設定を正しく解釈できませんでした。

### 解決方法  
1. **音声設定をリセットする**
2. **簡単なテキストで試す**
3. **最新バージョンの VoiceScriptPlayer を使用する**

---

## 💡 クレジット不足 (HailuoCreditError) {#credit-error}

### 原因  
- Hailuo アカウントの残高が不足しています。

### 解決方法  
1. **Hailuo アカウントの残高を確認する**
2. **チャージして再試行する**
3. **大量利用の場合は有料プランを検討する**

---

## 💡 不正リクエスト (HailuoRequestError) {#request-error}

### 原因  
- `voice_id`、`text`、`audio_setting` が無効な値になっています。

### 解決方法  
1. **テキストを簡潔にする**
2. **音声設定を初期化する**
3. **短い文で再試行する**

---

## 💡 サーバーエラー (HailuoServerError) {#server-error}

### 原因  
- Hailuo サーバー内部で問題が発生しました。

### 解決方法  
1. **時間をおいて再試行する**
2. **公式のお知らせを確認する**
3. **別の声で試してみる**

---

## 💡 レスポンス解析エラー (HailuoParseError) {#json-error}

### 原因  
- サーバーが予期しない形式のデータを返しました。

### 解決方法  
1. **再試行する**
2. **テキストを短くする**
3. **最新版のアプリを使用する**

---

## 💡 空の音声データ (HailuoEmptyAudio) {#empty-audio}

### 原因  
- テキストが短すぎる場合、音声が生成されないことがあります。

### 解決方法  
1. **もう少し長い文で試す**
2. **別の声で試す**
3. **再試行する**

---

## 💡 音声フォーマットエラー (HailuoInvalidAudioFormat) {#audio-format}

### 原因  
- サーバーから返された音声データが破損している可能性があります。

### 解決方法  
1. **再試行する**
2. **短いテキストでテストする**
3. **アプリの更新を確認する**

---

## 💡 音声処理失敗 (HailuoAudioProcessingError) {#process-error}

### 原因  
- 音声データのデコードに失敗しました。

### 解決方法  
1. **再試行する**
2. **別のテキストで試す**
3. **音声設定を変更する**

---

## 💡 残高不足 (HailuoInsufficientBalanceError) {#balance-error}

### 原因
- Hailuoアカウントのクレジットまたは残高が不足しているため、リクエストを処理できません。

### 解決方法
1. **アカウント残高の確認**
    - Hailuoダッシュボードで残高を確認してください。
2. **チャージを行う**
    - サービスを継続して利用するには、アカウントへのチャージが必要です。

---

## 💡 内部サーバーエラー (HailuoInternalError) {#internal-error}

### 原因
- Hailuoサーバー内部で予期せぬ問題が発生しました。

### 解決方法
1. **しばらくして再試行**
    - 一時的な障害である可能性が高いです。しばらく待ってから再度お試しください。
2. **ステータスの確認**
    - 問題が続く場合は、サービスのお知らせを確認してください。

---

## 💡 不適切な入力の検出 (HailuoSensitiveInputError) {#sensitive-input-error}

### 原因
- 入力されたテキストプロンプトに、ポリシー上許可されていない不適切な内容（政治、暴力、成人向けコンテンツなど）が含まれています。

### 解決方法
1. **プロンプトの修正**
    - 入力内容を見直し、不適切な単語や表現を修正または削除してください。

---

## 💡 不適切な出力の検出 (HailuoSensitiveOutputError) {#sensitive-output-error}

### 原因
- 生成された結果が不適切な内容と判断され、出力がブロックされました。

### 解決方法
1. **入力内容の調整**
    - 入力プロンプトを修正し、異なる結果が生成されるように誘導してください。

---

## 💡 システムエラー (HailuoSystemError) {#system-error}

### 原因
- データベースまたはシステム構成要素の問題により、リクエストが失敗しました。

### 解決方法
1. **再試行**
    - サーバー負荷などによる一時的な問題の可能性があります。しばらくしてから再試行してください。

---

## 💡 接続制限超過 (HailuoConnectionLimitError) {#connection-limit-error}

### 原因
- 同時に多数の接続リクエストがあったため、サーバーが処理を拒否しました。

### 解決方法
1. **リクエスト頻度の調整**
    - 同時リクエスト数を減らすか、少し時間を置いてからリクエストしてください。
2. **問い合わせ**
    - 問題が解決しない場合は、Hailuoサポートチームへの問い合わせが必要になる場合があります。

---

## 💡 ASR類似性チェック失敗 (HailuoASRCheckError) {#asr-check-error}

### 原因
- 提供されたオーディオファイルの音声と、入力された検証用テキストの内容が一致しません。

### 解決方法
1. **ファイルとテキストの確認**
    - アップロードしたオーディオ(`file_id`)と検証テキスト(`text_validation`)が正確に一致しているか確認してください。

---

## 💡 クローンプロンプトエラー (HailuoClonePromptError) {#clone-prompt-error}

### 原因
- 音声クローン（Voice Cloning）のためのプロンプトオーディオまたはテキストに問題があります。

### 解決方法
1. **オーディオ品質の確認**
    - プロンプトオーディオが明瞭で、背景ノイズがないか確認してください。
2. **プロンプトテキストの照合**
    - オーディオの内容とプロンプトテキストが正確に一致しているか確認してください。

---

## 💡 パラメータエラー (HailuoInvalidParamsError) {#invalid-params-error}

### 原因
- APIリクエストに含まれる設定値やJSON構造が正しくありません。

### 解決方法
1. **リクエストパラメータの検討**
    - 送信するデータのフィールド名や値の形式がAPI仕様書と一致しているか確認してください。

---

## 💡 無効なVoice ID (HailuoInvalidVoiceIDError) {#invalid-voiceid-error}

### 原因
- リクエストしたVoice IDが存在しないか、形式が間違っています。
- 使用できないサンプルを参照しています。

### 解決方法
1. **IDの確認**
    - 使用しようとしているモデルまたはボイスのIDが正しいか、リストで再確認してください。

---

## 💡 音声の長さエラー (HailuoDurationError) {#duration-error}

### 原因
- アップロードした音声ファイルの長さが短すぎるか、長すぎます。

### 解決方法
1. **ファイルの長さを調整**
    - 音声クローンに適した長さ（推奨時間を確認してください）にオーディオを編集し、再試行してください。

---

## 💡 Voice IDの重複 (HailuoDuplicateVoiceIDError) {#duplicate-voiceid-error}

### 原因
- 作成しようとしているVoice IDは既にシステムに存在します。

### 解決方法
1. **IDの変更**
    - 別のVoice IDを使用して作成を試みてください。

---

## 💡 アクセス権限なし (HailuoAccessDeniedError) {#access-denied-error}

### 原因
- そのVoice IDを使用する権限がありません。（自身が作成していない、または共有されていない）

### 解決方法
1. **所有権の確認**
    - 自身が作成したボイスか、または公開されているボイスか確認してください。

---

## 💡 リクエスト増加率制限 (HailuoRateGrowthError) {#rate-growth-error}

### 原因
- 短時間にリクエスト量が急激に増加したため、システム保護のためにブロックされました。

### 解決方法
1. **リクエスト速度の調整**
    - 急激な大量リクエストを避け、徐々にリクエスト頻度を増やしてください。

---

## 💡 プロンプトオーディオ長超過 (HailuoPromptAudioTooLongError) {#prompt-too-long-error}

### 原因
- 音声クローンに使用されたプロンプトオーディオが、許可された長さを超えています。

### 解決方法
1. **オーディオのトリミング**
    - プロンプトオーディオファイルを**8秒未満**に編集し、再度アップロードしてください。

---

## 💡 無効なAPIキー (HailuoInvalidApiKeyError) {#invalid-apikey-error}

### 原因
- APIキーの形式が間違っているか、無効化されたキーです。

### 解決方法
1. **APIキーの再確認**
    - キーが正しいか確認し、期限切れの場合は再発行してください。

---

## 💡 使用量制限超過 (HailuoUsageLimitExceededError) {#usage-limit-error}

### 原因
- 該当期間（時間/日/月）に許可されたAPI呼び出し回数または使用量を使い切りました。

### 解決方法
1. **リソース更新待ち**
    - 次の更新サイクル（通常は5時間ウィンドウ）までお待ちください。

---

# hailuo-unexpected  
## 💡 予期しない応答 (HailuoUnexpectedResponse) {#unexpected}

### 原因  
- 必要なフィールドが欠落している、またはサーバー応答が不規則です。

### 解決方法  
1. **時間をおいて再試行する**
2. **テキストを短くする**
3. **VoiceScriptPlayer を最新版に更新する**

---

# hailuo-unknown  
## 💡 不明なエラー (HailuoUnknownError) {#unknown-error}

### 原因  
- 予期しない問題が発生しました。

### 解決方法  
1. **アプリを再起動する**
2. **設定をリセットして再試行する**
3. **問題が続く場合は開発者に連絡する**
